# Root Environment Configuration
# This file documents environment variables needed for the entire project

# ==============================================================================
# BACKEND ENVIRONMENT VARIABLES (backend/.env)
# ==============================================================================
# Copy backend/.env.example to backend/.env and configure these:
#
# SECRET_KEY=your-secret-key-here
# DEBUG=False
# ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
#
# # Database (external location)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=your_db_host
# DB_PORT=5432
#
# # CORS
# CORS_ALLOWED_ORIGINS=http://localhost:4200,https://your-domain.com

# ==============================================================================
# FRONTEND ENVIRONMENT VARIABLES
# ==============================================================================
# Frontend environment variables are configured in:
# - frontend/src/environments/environment.ts (development)
# - frontend/src/environments/environment.prod.ts (production)

# ==============================================================================
# CI/CD GITHUB SECRETS
# ==============================================================================
# Configure these in GitHub repository settings:
# Settings → Secrets and variables → Actions
#
# SERVER_HOST=your-server-ip-or-domain
# SERVER_USER=deployment-user
# SERVER_PORT=22
# SSH_PRIVATE_KEY=<paste-ssh-private-key>
# DEPLOY_PATH=/path/to/deployment/directory
# SECRET_KEY=<django-secret-key>
# API_URL=https://your-domain.com/api

# ==============================================================================
# NOTES
# ==============================================================================
# - Never commit .env files to version control
# - Keep database credentials secure and external
# - Use strong, unique SECRET_KEY in production
# - Generate SSH keys specifically for deployment
# - Store all sensitive data in external secure location
