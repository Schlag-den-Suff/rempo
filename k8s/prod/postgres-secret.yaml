apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: knowker-prod
type: Opaque
stringData:
  # CRITICAL: You MUST change this password before deploying to production!
  # Generate a strong password with at least 16 characters:
  # Use: openssl rand -base64 32
  # Or a password manager to generate a secure password
  POSTGRES_PASSWORD: "CHANGE_THIS_PASSWORD_BEFORE_PRODUCTION"
